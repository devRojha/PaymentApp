# For start locally

Method-1 For Docker user:

```bash
docker build -t BackDev .
docker run -d -p 8080:8080 --name PaymentBack backDev
```
Method-2 For Normal:

```bash
npm install
node index.js
```
